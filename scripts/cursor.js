/* ============================================================
    CURSOR.JS — Cursor personalizado (preparado para futura implementación)
   ============================================================ */

function initCustomCursor() {
    // Actualmente no se usa cursor personalizado.
    // Este archivo está preparado para añadir uno en el futuro.
    // Ejemplo de uso: crear un div .cursor-dot que siga al ratón.

    /*
    const dot = document.createElement('div');
    dot.classList.add('cursor-dot');
    document.body.appendChild(dot);

    document.addEventListener('mousemove', (e) => {
        dot.style.left = e.clientX + 'px';
        dot.style.top  = e.clientY + 'px';
    });
    */
}

document.addEventListener('DOMContentLoaded', initCustomCursor);